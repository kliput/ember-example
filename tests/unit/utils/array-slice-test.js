import arraySlice from 'ember-example/utils/array-slice';
import { module, test } from 'qunit';

module('Unit | Utility | array slice');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arraySlice();
  assert.ok(result);
});
